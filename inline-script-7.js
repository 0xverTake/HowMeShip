const apiConfig = {
        shipsUrl: "/ajax/getShips", /** Replace this with a function which returns the symfony api url. */
        itemsUrl: "/ajax/getPath"  /** Replace this with a function which returns the necessary transformation items. */
    };